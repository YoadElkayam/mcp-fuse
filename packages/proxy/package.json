{
  "name": "mcp-fuse",
  "version": "0.1.1",
  "description": "Zero-config MCP resilience proxy: wrap any MCP server, stop token-burning error loops",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YoadElkayam/mcp-fuse.git",
    "directory": "packages/proxy"
  },
  "homepage": "https://github.com/YoadElkayam/mcp-fuse#readme",
  "bugs": "https://github.com/YoadElkayam/mcp-fuse/issues",
  "keywords": ["mcp", "model-context-protocol", "proxy", "resilience", "retry", "circuit-breaker", "rate-limit", "llm", "agents", "claude", "cursor"],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp-fuse": "dist/cli.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json && node --test dist-test/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "mcp-fuse-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.15.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
