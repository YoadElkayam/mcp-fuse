{
  "name": "@mcp-fuse/example-hostile-server",
  "version": "0.0.1",
  "private": true,
  "description": "Conformance fixture: an MCP server that produces every ambiguous failure outcome around a side-effecting tool and counts real executions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json && node --test dist-test/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "conformance": "node dist/run.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "mcp-fuse": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
