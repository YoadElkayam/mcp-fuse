{
  "name": "mcp-fuse-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Deterministic failure handling for MCP: the Error Policy standard + a zero-config resilience proxy",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist dist-test *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
