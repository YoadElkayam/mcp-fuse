---
name: Bug report
about: The proxy broke, hung, or changed behavior it shouldn't have
title: ""
labels: ["bug"]
---

**What happened:**

**Expected:**

**Setup** (host + version, wrapped server, mcp-fuse version, OS):

**Repro** — ideally the `wrap` command line and, if possible, the JSONL audit log
(`--log-file`) around the failure:

```
```
